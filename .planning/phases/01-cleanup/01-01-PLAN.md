---
phase: 01-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - terminair/screens/pools.py
  - terminair/screens/health.py
  - terminair/screens/sla_misses.py
  - terminair/screens/resource_timeline.py
  - terminair/screens/xcom_viewer.py
  - terminair/screens/dags.py
  - terminair/screens/dag_detail.py
  - terminair/screens/dag_deps.py
  - terminair/screens/dag_graph.py
  - terminair/screens/task_instances.py
  - terminair/screens/task_history.py
  - terminair/screens/broken_summary.py
  - terminair/screens/recent_activity.py
  - terminair/screens/event_log.py
  - terminair/screens/import_errors.py
  - terminair/screens/watchlist.py
  - terminair/api/client.py
  - terminair/api/poller.py
  - terminair/metrics/aggregations.py
  - terminair/metrics/critical_path.py
  - terminair/metrics/error_extract.py
  - terminair/metrics/sparkline.py
  - terminair/export.py
  - terminair/tests/test_metrics.py
  - terminair/tests/test_failure_heatmap.py
  - terminair/tests/test_event_log_loader.py
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03

must_haves:
  truths:
    - "No Airflow screen files exist under terminair/screens/"
    - "No deprecated API or metrics source modules exist under terminair/api/ or terminair/metrics/"
    - "No test files for removed code exist under terminair/tests/"
  artifacts:
    - path: "terminair/screens/"
      provides: "Empty screens directory (no Airflow .py files remain)"
      contains: "only __init__.py if present — no pools, health, sla_misses, resource_timeline, xcom_viewer, dags, dag_detail, dag_deps, dag_graph, task_instances, task_history, broken_summary, recent_activity, event_log, import_errors, watchlist"
    - path: "terminair/api/"
      provides: "API directory with only models.py and auth/ — no client.py or poller.py"
    - path: "terminair/metrics/"
      provides: "Directory absent or empty — no aggregations.py, critical_path.py, error_extract.py, sparkline.py"
  key_links:
    - from: "terminair/api/"
      to: "client.py and poller.py"
      via: "file deletion"
      pattern: "client\\.py|poller\\.py"
    - from: "terminair/tests/"
      to: "test_metrics.py, test_failure_heatmap.py, test_event_log_loader.py"
      via: "file deletion"
      pattern: "test_metrics|test_failure_heatmap|test_event_log_loader"
---

<objective>
Delete all deprecated Airflow screen files, source modules, and associated tests from the codebase.

Purpose: Provide a clean foundation for the dbt rewrite. These files have no future role — deleting them (not archiving) is the correct action.
Output: 26 files deleted. The screens/, api/, metrics/ directories may partially remain for files that are kept (models.py, auth/).
</objective>

<execution_context>
@/Users/chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/chaitanya/Documents/github/terminair/.planning/PROJECT.md
@/Users/chaitanya/Documents/github/terminair/.planning/ROADMAP.md
@/Users/chaitanya/Documents/github/terminair/.planning/STATE.md
@/Users/chaitanya/Documents/github/terminair/.planning/phases/01-cleanup/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all deprecated Airflow screen files</name>
  <files>
    terminair/screens/pools.py
    terminair/screens/health.py
    terminair/screens/sla_misses.py
    terminair/screens/resource_timeline.py
    terminair/screens/xcom_viewer.py
    terminair/screens/dags.py
    terminair/screens/dag_detail.py
    terminair/screens/dag_deps.py
    terminair/screens/dag_graph.py
    terminair/screens/task_instances.py
    terminair/screens/task_history.py
    terminair/screens/broken_summary.py
    terminair/screens/recent_activity.py
    terminair/screens/event_log.py
    terminair/screens/import_errors.py
    terminair/screens/watchlist.py
  </files>
  <read_first>
    Read terminair/screens/ directory listing to confirm all 16 files are present before deleting.
    Do NOT read each file — a directory listing is sufficient; content is irrelevant for deletion.
  </read_first>
  <action>
    Delete all 16 Airflow screen files from terminair/screens/ using a single rm command:

      rm terminair/screens/pools.py \
         terminair/screens/health.py \
         terminair/screens/sla_misses.py \
         terminair/screens/resource_timeline.py \
         terminair/screens/xcom_viewer.py \
         terminair/screens/dags.py \
         terminair/screens/dag_detail.py \
         terminair/screens/dag_deps.py \
         terminair/screens/dag_graph.py \
         terminair/screens/task_instances.py \
         terminair/screens/task_history.py \
         terminair/screens/broken_summary.py \
         terminair/screens/recent_activity.py \
         terminair/screens/event_log.py \
         terminair/screens/import_errors.py \
         terminair/screens/watchlist.py

    The terminair/screens/ directory itself stays — new dbt screens go there in Phase 4.
    Do NOT delete any widget files, theme files, or __init__.py.
  </action>
  <verify>
    <automated>ls terminair/screens/ | grep -E "pools|health|sla_misses|resource_timeline|xcom_viewer|dags|dag_detail|dag_deps|dag_graph|task_instances|task_history|broken_summary|recent_activity|event_log|import_errors|watchlist" | wc -l | grep -x "0"</automated>
  </verify>
  <acceptance_criteria>
    - `ls terminair/screens/` returns zero lines matching any of the 16 deleted screen names
    - `terminair/screens/` directory still exists (not deleted itself)
    - No widget or theme files were touched
  </acceptance_criteria>
  <done>All 16 Airflow screen .py files are absent from terminair/screens/</done>
</task>

<task type="auto">
  <name>Task 2: Delete deprecated API modules, metrics modules, export.py, and test files</name>
  <files>
    terminair/api/client.py
    terminair/api/poller.py
    terminair/metrics/aggregations.py
    terminair/metrics/critical_path.py
    terminair/metrics/error_extract.py
    terminair/metrics/sparkline.py
    terminair/export.py
    terminair/tests/test_metrics.py
    terminair/tests/test_failure_heatmap.py
    terminair/tests/test_event_log_loader.py
  </files>
  <read_first>
    Run `ls terminair/api/ terminair/metrics/ terminair/tests/` to confirm the target files exist before deleting.
    Confirm terminair/api/models.py and terminair/api/auth/ are present — they must NOT be deleted.
  </read_first>
  <action>
    Delete the 10 target files in two groups:

    API and metrics modules (must keep models.py and auth/):
      rm terminair/api/client.py \
         terminair/api/poller.py \
         terminair/metrics/aggregations.py \
         terminair/metrics/critical_path.py \
         terminair/metrics/error_extract.py \
         terminair/metrics/sparkline.py \
         terminair/export.py

    Test files for removed code (must NOT delete conftest.py, test_config.py, test_flash.py, test_command_palette.py, test_read_only.py):
      rm terminair/tests/test_metrics.py \
         terminair/tests/test_failure_heatmap.py \
         terminair/tests/test_event_log_loader.py

    After deleting all metrics files, remove the now-empty metrics directory:
      rmdir terminair/metrics/ 2>/dev/null || true

    IMPORTANT: Verify terminair/api/models.py still exists after the rm commands.
  </action>
  <verify>
    <automated>
      python3 -c "
import os, sys
missing_ok = [
    'terminair/api/client.py',
    'terminair/api/poller.py',
    'terminair/export.py',
    'terminair/tests/test_metrics.py',
    'terminair/tests/test_failure_heatmap.py',
    'terminair/tests/test_event_log_loader.py',
]
must_exist = [
    'terminair/api/models.py',
    'terminair/api/auth',
    'terminair/tests/conftest.py',
    'terminair/tests/test_config.py',
    'terminair/tests/test_flash.py',
    'terminair/tests/test_command_palette.py',
    'terminair/tests/test_read_only.py',
]
errors = []
for p in missing_ok:
    if os.path.exists(p):
        errors.append(f'SHOULD BE DELETED: {p}')
for p in must_exist:
    if not os.path.exists(p):
        errors.append(f'MUST EXIST: {p}')
if errors:
    print('\n'.join(errors)); sys.exit(1)
print('All deletions and preservations verified.')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - terminair/api/client.py does not exist
    - terminair/api/poller.py does not exist
    - terminair/metrics/ directory does not exist (all 4 files deleted, rmdir succeeded)
    - terminair/export.py does not exist
    - terminair/tests/test_metrics.py does not exist
    - terminair/tests/test_failure_heatmap.py does not exist
    - terminair/tests/test_event_log_loader.py does not exist
    - terminair/api/models.py still exists
    - terminair/api/auth/ directory still exists
    - terminair/tests/conftest.py, test_config.py, test_flash.py, test_command_palette.py, test_read_only.py all still exist
  </acceptance_criteria>
  <done>All 10 deprecated source/test files deleted; all kept files remain intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem | File deletions are irreversible — must not delete kept files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | rm commands | mitigate | Explicit file list in rm commands (no wildcards); acceptance criteria verify kept files still exist |
| T-01-02 | Denial of Service | rmdir metrics/ | accept | rmdir only removes empty directories; fails safely if non-empty with `|| true` |
</threat_model>

<verification>
After both tasks complete:
- `ls terminair/screens/` shows zero files matching any deleted screen name
- `ls terminair/api/` shows models.py and auth/ only (no client.py or poller.py)
- `ls terminair/metrics/` fails (directory removed) or shows empty
- `ls terminair/tests/` does NOT include test_metrics.py, test_failure_heatmap.py, test_event_log_loader.py
- Kept test files still present: conftest.py, test_config.py, test_flash.py, test_command_palette.py, test_read_only.py
</verification>

<success_criteria>
All 26 deprecated files are deleted. The terminair/screens/, terminair/api/, and terminair/tests/ directories remain (containing only kept files). terminair/metrics/ is removed entirely. No kept file was touched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup/01-01-SUMMARY.md` using the summary template.
</output>
