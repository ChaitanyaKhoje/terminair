---
phase: 05-tests-+-build
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - terminair/tests/dbt/test_regression.py
  - terminair/tests/dbt/test_mock_data.py
  - terminair/tests/dbt/test_regression_and_mock.py
  - terminair/tests/test_read_only.py
  - Dockerfile
autonomous: true
requirements:
  - TST-01
  - TST-02
  - TST-03
  - TST-04
  - TST-05
  - BLD-01
  - BLD-02
  - BLD-03

must_haves:
  truths:
    - "pytest collects test_regression.py with all 6 signal types including upstream_schema_change"
    - "pytest collects test_mock_data.py with all MockDataProvider tick() and status tests"
    - "test_regression_and_mock.py is deleted — no duplicate test IDs"
    - "test_read_only.py asserts AirflowBridge has zero write methods (no longer a placeholder)"
    - "Dockerfile CMD uses AIRFLOW_URL when set; falls back to --demo when unset"
    - "Full test suite passes after all four changes"
  artifacts:
    - path: "terminair/tests/dbt/test_regression.py"
      provides: "RegressionAnalyzer tests for all 6 signal types"
      contains: "upstream_schema_change"
    - path: "terminair/tests/dbt/test_mock_data.py"
      provides: "MockDataProvider tick() transition and signal coverage tests"
      contains: "TestMockDataProvider"
    - path: "terminair/tests/test_read_only.py"
      provides: "AirflowBridge write-method enforcement"
      contains: "test_airflow_bridge_has_no_write_methods"
    - path: "Dockerfile"
      provides: "Configurable container entrypoint"
      contains: "AIRFLOW_URL"
  key_links:
    - from: "terminair/tests/dbt/test_regression.py"
      to: "terminair/dbt/regression.py"
      via: "RegressionAnalyzer([consumer, curr_upstream]).analyze(prev_models=[prev_upstream])"
      pattern: "upstream_schema_change"
    - from: "terminair/tests/test_read_only.py"
      to: "terminair/dbt/airflow_bridge.AirflowBridge"
      via: "inspect.getmembers(AirflowBridge, predicate=inspect.isfunction)"
      pattern: "write_methods.*post.*put.*delete.*patch"
    - from: "Dockerfile CMD"
      to: "python -m terminair --url"
      via: "shell-form CMD with $AIRFLOW_URL env var expansion"
      pattern: "AIRFLOW_URL"
---

<objective>
Close the four gaps between Phase 5 success criteria and the current codebase state:

1. Split test_regression_and_mock.py into test_regression.py (with upstream_schema_change added) and test_mock_data.py, then delete the source to eliminate duplicate test IDs.
2. Replace the test_read_only.py placeholder with a real AirflowBridge write-method assertion.
3. Fix Dockerfile CMD to conditionally wire $AIRFLOW_URL to --url rather than hardcoding --demo.

TST-01, TST-03, BLD-01, BLD-02 are already satisfied — the plan verifies them but does not re-implement.

Purpose: Bring all 8 Phase 5 requirements to SATISFIED so the phase gate passes.
Output: 2 new test files, 1 deleted file, 1 modified test file, 1 modified Dockerfile. Test count increases by 1 (upstream_schema_change test); all existing 112 tests continue to pass.
</objective>

<execution_context>
@/Users/chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tests-+-build/05-RESEARCH.md
@.planning/phases/05-tests-+-build/05-PATTERNS.md
</context>

<interfaces>
From terminair/dbt/regression.py (upstream_schema_change logic, lines 162-196):

The signal fires when, for each dep_id in model.upstream_deps:
- curr_dep (in self._current) and prev_dep (in prev_map) both exist
- curr_dep.materialization != prev_dep.materialization OR curr_dep.grain_columns != prev_dep.grain_columns

Key: upstream_deps must be populated on the consumer ModelState (default is []). Pass
prev_models=[...] to analyze() so the prev_map is built.

From terminair/tests/dbt/test_airflow_bridge.py (write-method check pattern, lines 31-48):

    import inspect
    members = dict(inspect.getmembers(AirflowBridge, predicate=inspect.isfunction))
    write_methods = [m for m in members if m in ("post", "put", "delete", "patch")]
    assert not write_methods, f"Found write methods: {write_methods}"

Correct import path: from terminair.dbt.airflow_bridge import AirflowBridge
(not terminair.api.airflow_bridge — that path does not exist)

From terminair/tests/dbt/test_regression_and_mock.py (existing TestRegressionAnalyzer and
TestMockDataProvider classes — these are moved verbatim, not rewritten):
- TestRegressionAnalyzer: lines 13-315 (10 test methods covering 5 signal types + import + sort + signals_for_model)
- TestMockDataProvider: lines 317-446 (12 test methods covering tick, distributions, copy semantics)

ModelState required fields (no defaults — must always be provided):
    node_id, name, tag, status, dag_id, task_id, materialization,
    schema_name, database_name, has_upstream_failure
Optional for upstream_schema_change test: upstream_deps=["model.p.dep_node_id"]
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split test_regression_and_mock.py and add upstream_schema_change test</name>
  <files>
    terminair/tests/dbt/test_regression.py,
    terminair/tests/dbt/test_mock_data.py,
    terminair/tests/dbt/test_regression_and_mock.py
  </files>
  <read_first>
    - terminair/tests/dbt/test_regression_and_mock.py — full file (source for both new files)
    - terminair/dbt/regression.py lines 162-196 — upstream_schema_change implementation
    - terminair/dbt/models.py — ModelState fields (to construct test fixture correctly)
  </read_first>
  <behavior>
    - test_regression.py contains TestRegressionAnalyzer with all existing 10 test methods PLUS one new test: test_upstream_schema_change_warning
    - test_upstream_schema_change_warning: construct prev_upstream (materialization="view") and curr_upstream (materialization="table"); construct consumer with upstream_deps=["model.p.stg_orders"]; call RegressionAnalyzer([consumer, curr_upstream]).analyze(prev_models=[prev_upstream]); assert exactly 1 signal with signal_type=="upstream_schema_change" and severity==Severity.WARNING and node_id=="model.p.fct_orders"
    - test_mock_data.py contains TestMockDataProvider with all existing 12 test methods verbatim
    - test_regression_and_mock.py is DELETED after both new files are confirmed to collect cleanly
    - pytest --collect-only shows no duplicate test IDs
  </behavior>
  <action>
    1. Create terminair/tests/dbt/test_regression.py:
       - Header docstring: "Tests for RegressionAnalyzer — all 6 signal types and severity thresholds."
       - Imports: from __future__ import annotations; import pytest; from terminair.dbt.models import Severity
       - Copy TestRegressionAnalyzer class verbatim from test_regression_and_mock.py (lines 13-315)
       - Add test_upstream_schema_change_warning method to the class per the behavior block above (upstream_deps pitfall: consumer's upstream_deps MUST list the dep's node_id or the loop at regression.py:165 never fires)

    2. Create terminair/tests/dbt/test_mock_data.py:
       - Header docstring: "Tests for MockDataProvider — tick() transitions, row_delta_pct recomputation, signal coverage."
       - Imports: from __future__ import annotations; import asyncio; import inspect
       - Copy TestMockDataProvider class verbatim from test_regression_and_mock.py (lines 317-446)

    3. Run: .venv/bin/python -m pytest terminair/tests/dbt/test_regression.py terminair/tests/dbt/test_mock_data.py --collect-only -q
       Confirm zero duplicate test IDs and that both files collect without import errors.

    4. Delete terminair/tests/dbt/test_regression_and_mock.py to eliminate duplicate collection.

    5. Run full suite: .venv/bin/python -m pytest terminair/tests/ -q
       Confirm the total count is 113 (112 existing + 1 new upstream_schema_change test) and 0 failures.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest terminair/tests/dbt/test_regression.py -x -q && .venv/bin/python -m pytest terminair/tests/dbt/test_mock_data.py -x -q && .venv/bin/python -m pytest terminair/tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - terminair/tests/dbt/test_regression.py exists and pytest collects it (11 test methods including test_upstream_schema_change_warning)
    - terminair/tests/dbt/test_mock_data.py exists and pytest collects it (12 test methods)
    - terminair/tests/dbt/test_regression_and_mock.py does NOT exist
    - Full suite passes with count >= 113 and 0 failures
    - No duplicate test IDs in --collect-only output
  </acceptance_criteria>
  <done>pytest terminair/tests/ passes with no failures; test_regression.py has 11 tests (5 signal types + upstream_schema_change + import + sort + signals_for_model + 2 threshold variants); test_mock_data.py has 12 tests; test_regression_and_mock.py is gone</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace test_read_only.py placeholder with AirflowBridge write-method assertion</name>
  <files>
    terminair/tests/test_read_only.py
  </files>
  <read_first>
    - terminair/tests/test_read_only.py — current placeholder (to understand what to replace)
    - terminair/tests/dbt/test_airflow_bridge.py lines 31-48 — inspect pattern to copy
    - terminair/dbt/airflow_bridge.py — confirm correct import path and zero write methods
  </read_first>
  <behavior>
    - test_airflow_bridge_has_no_write_methods: imports AirflowBridge from terminair.dbt.airflow_bridge; uses inspect.getmembers to list all functions; asserts none of ("post", "put", "delete", "patch") appear
    - The placeholder function test_placeholder_read_only_contract is removed entirely
    - Test passes when run against the current AirflowBridge (which has zero write methods per Phase 2)
  </behavior>
  <action>
    Replace the entire contents of terminair/tests/test_read_only.py with:

    Module docstring: "Tests to enforce read-only contract for AirflowBridge."
    Imports: from __future__ import annotations; import inspect
    Function test_airflow_bridge_has_no_write_methods() implementing the inspect-based assertion.

    Use the exact import path: from terminair.dbt.airflow_bridge import AirflowBridge
    (wrong path terminair.api.airflow_bridge does not exist and will raise ImportError)

    Do not duplicate the source-level grep assertion from test_airflow_bridge.py — the inspect-based
    assertion is sufficient for test_read_only.py to satisfy TST-05.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest terminair/tests/test_read_only.py -v</automated>
  </verify>
  <acceptance_criteria>
    - test_read_only.py contains exactly one test: test_airflow_bridge_has_no_write_methods
    - The test imports from terminair.dbt.airflow_bridge (not terminair.api.*)
    - pytest test_read_only.py -v shows PASSED (not "pass" placeholder)
    - No ImportError or AttributeError
  </acceptance_criteria>
  <done>pytest terminair/tests/test_read_only.py -v shows 1 test PASSED; grep confirms "inspect" and "AirflowBridge" are present and "placeholder" is absent</done>
</task>

<task type="auto">
  <name>Task 3: Fix Dockerfile CMD to wire AIRFLOW_URL and verify BLD-01/BLD-02/BLD-03</name>
  <files>
    Dockerfile
  </files>
  <read_first>
    - Dockerfile — current 17-line file (CMD is the only line that changes)
    - terminair/cli.py lines 55-70 — confirm TERMINAIR_PASSWORD env var handling (line 60 already reads it)
  </read_first>
  <action>
    Replace only the CMD line in Dockerfile. Keep all other lines unchanged.

    Old CMD (exec-form, hardcoded --demo):
      CMD ["python", "-m", "terminair", "--demo"]

    New CMD (shell-form, conditional on AIRFLOW_URL and TERMINAIR_DEMO):
      CMD if [ -n "$AIRFLOW_URL" ] && [ "$TERMINAIR_DEMO" != "1" ]; then \
            exec python -m terminair --url "$AIRFLOW_URL" ${TERMINAIR_USER:+--user "$TERMINAIR_USER"}; \
          else \
            exec python -m terminair --demo; \
          fi

    Password note: cli.py line 60 already reads TERMINAIR_PASSWORD env var. The non-demo path
    requires TERMINAIR_PASSWORD to be set in the container environment to avoid an interactive
    prompt blocking non-TTY containers.

    After editing the Dockerfile, verify BLD-01 and BLD-02 are still satisfied:
      grep "dbt-demo:" Makefile
      grep "dbt-dev:" Makefile

    Verify BLD-03 (Dockerfile builds successfully):
      docker build -t terminair-test . 2>&1 | tail -5

    If docker is unavailable, note it and skip the build check — the CMD syntax is valid shell.
  </action>
  <verify>
    <automated>grep -c "AIRFLOW_URL" Dockerfile && grep "dbt-demo:" Makefile && grep "dbt-dev:" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile CMD is shell-form and references $AIRFLOW_URL
    - When AIRFLOW_URL is set and TERMINAIR_DEMO != "1", CMD passes --url "$AIRFLOW_URL"
    - When AIRFLOW_URL is unset or TERMINAIR_DEMO=1, CMD falls back to --demo
    - docker build -t terminair-test . succeeds (or is noted as skipped with reason)
    - grep "dbt-demo:" Makefile returns a match (BLD-01 verified)
    - grep "dbt-dev:" Makefile returns a match (BLD-02 verified)
    - VOLUME ["/app/target"] line unchanged (BLD-03 mount requirement preserved)
  </acceptance_criteria>
  <done>Dockerfile CMD conditionally uses $AIRFLOW_URL; docker build succeeds (or is skipped with documented reason); Makefile dbt-demo and dbt-dev targets confirmed present; VOLUME line unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container env → CLI args | AIRFLOW_URL and TERMINAIR_USER values from container environment are passed directly to CLI flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Dockerfile CMD TERMINAIR_PASSWORD | accept | Password passed via env var (not CLI arg) so it does not appear in process list; same risk exists in all containerised Python apps; low severity for a local dev tool |
| T-05-02 | Tampering | test_read_only.py import path | mitigate | Import is hardcoded to terminair.dbt.airflow_bridge; any path change causes ImportError, making the test fail-safe |
</threat_model>

<verification>
After all three tasks complete, run the full suite once:

  .venv/bin/python -m pytest terminair/tests/ -v 2>&1 | tail -20

Expected outcome:
- Count: >= 113 tests (112 baseline + 1 upstream_schema_change)
- Result: 0 failures, 0 errors
- Files collected: test_manifest.py, test_aggregator.py, test_regression.py, test_mock_data.py, test_artifacts.py, test_airflow_bridge.py, test_snowflake_client.py, test_read_only.py, test_config.py, test_app_demo.py, test_cli.py, test_phase5_packaging.py
- test_regression_and_mock.py: NOT in collected list (deleted)

Spot-check TST-01 (already satisfied, verify only):
  .venv/bin/python -m pytest terminair/tests/dbt/test_manifest.py -q

Spot-check TST-03 (already satisfied, verify only):
  .venv/bin/python -m pytest terminair/tests/dbt/test_aggregator.py -q

Spot-check BLD-01:
  grep "dbt-demo:" Makefile

Spot-check BLD-02:
  grep "dbt-dev:" Makefile
</verification>

<success_criteria>
All 8 requirements satisfied:

| Req | Criterion | Verification |
|-----|-----------|--------------|
| TST-01 | test_manifest.py covers all ManifestLoader methods | pytest test_manifest.py passes |
| TST-02 | test_regression.py covers all 6 signal types including upstream_schema_change | pytest test_regression.py -v shows 11 tests PASSED |
| TST-03 | test_aggregator.py covers has_upstream_failure | pytest test_aggregator.py passes |
| TST-04 | test_mock_data.py covers tick() transitions | pytest test_mock_data.py -v shows 12 tests PASSED |
| TST-05 | test_read_only.py asserts AirflowBridge has no write methods | pytest test_read_only.py -v shows test_airflow_bridge_has_no_write_methods PASSED |
| BLD-01 | Makefile dbt-demo target present | grep "dbt-demo:" Makefile returns match |
| BLD-02 | Makefile dbt-dev target present | grep "dbt-dev:" Makefile returns match |
| BLD-03 | Dockerfile builds and wires AIRFLOW_URL | docker build succeeds; grep AIRFLOW_URL Dockerfile returns match |
</success_criteria>

<output>
After completion, create .planning/phases/05-tests-+-build/05-01-SUMMARY.md using the template at ~/.claude/get-shit-done/templates/summary.md.

Key items to capture in the summary:
- Final test count (expected: 113+)
- Confirmation that test_regression_and_mock.py was deleted
- Dockerfile CMD form used (shell-form conditional)
- Any deviation from the plan (e.g., docker build skipped)
</output>
