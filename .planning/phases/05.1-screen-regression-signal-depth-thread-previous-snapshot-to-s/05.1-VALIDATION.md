---
phase: "05.1"
slug: screen-regression-signal-depth
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-05-16
---

# Phase 05.1 — Validation Strategy

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (uv run pytest) |
| **Quick run command** | `uv run pytest terminair/tests/ -q --tb=short` |
| **Full suite command** | `uv run pytest terminair/tests/ -v --tb=short` |
| **Estimated runtime** | ~1 second |

## Sampling Rate

- **After every task commit:** `uv run pytest terminair/tests/ -q --tb=short`
- **Before verification:** Full suite must be green (≥113 tests)

## Per-Task Verification Map

| Task | Requirement | Automated Command | Status |
|------|-------------|-------------------|--------|
| Add get_previous_models() to providers | DAT-06 | `uv run pytest terminair/tests/dbt/ -q` | ⬜ pending |
| Update screens to pass previous models | SCR-01/02/04 | `uv run pytest terminair/tests/ -q` | ⬜ pending |
| Fix clock on initial mount | SCR-01 | `uv run pytest terminair/tests/ -q` | ⬜ pending |

## Wave 0 Requirements

Existing infrastructure covers all requirements. 113 tests passing before this phase.

## Manual-Only Verifications

| Behavior | Why Manual | Instructions |
|----------|------------|--------------|
| grain_added/removed/upstream_schema_change visible in ProblemsScreen | Requires live TUI with configured run_results_previous_path | `make dbt-demo` → press 2 → verify 6 signal types shown |
| Clock ticks immediately on first ModelListScreen launch | Requires live TUI | `make dbt-demo` → verify clock ticks from first display |
