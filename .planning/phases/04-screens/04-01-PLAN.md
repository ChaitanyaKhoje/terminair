---
phase: 04-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - terminair/screens/model_list.py
  - terminair/screens/lineage.py
  - terminair/screens/detail.py
autonomous: true
requirements: [SCR-01, SCR-02, SCR-03, SCR-04, SCR-05]

must_haves:
  truths:
    - "ModelListScreen header shows connection URL and live clock"
    - "ModelListScreen bottom statusbar shows model count and regression warning count"
    - "LineageScreen defaults to 4-hop depth on first render"
    - "ModelDetailScreen keys 1-5 switch to the corresponding tab"
    - "ModelDetailScreen SQL tab content is scrollable with VerticalScroll"
    - "SCR-02 ProblemsScreen two-section layout and SCR-05 shared bindings are unmodified and still passing"
  artifacts:
    - path: "terminair/screens/model_list.py"
      provides: "ModelListScreen with header clock + bottom statusbar"
      contains: "_update_header"
    - path: "terminair/screens/lineage.py"
      provides: "LineageScreen with _depth=4 default"
      contains: "_depth = 4"
    - path: "terminair/screens/detail.py"
      provides: "ModelDetailScreen with 1-5 BINDINGS and VerticalScroll SQL pane"
      contains: "VerticalScroll"
  key_links:
    - from: "ModelListScreen.on_mount"
      to: "_update_header"
      via: "self.set_interval(1.0, self._update_header)"
      pattern: "set_interval.*_update_header"
    - from: "ModelDetailScreen.BINDINGS"
      to: "TabbedContent.active"
      via: "action_switch_tab(tab_id)"
      pattern: "switch_tab.*tab-"
    - from: "detail.py compose()"
      to: "VerticalScroll"
      via: "TabPane(\"SQL\") wrapping VerticalScroll"
      pattern: "VerticalScroll.*detail-sql-scroll"
---

<objective>
Close the four concrete gaps between the pre-implemented screen files and the ROADMAP Phase 4 success criteria. All four dbt screens already exist; this plan makes surgical edits to three files to meet the remaining requirements.

SCR-02 (ProblemsScreen) and SCR-05 (shared DbtScreen bindings) are fully met and must not be changed.

Purpose: Bring all five SCR requirements to completion before the phase 5 test suite is built.
Output: Three modified screen files. No new files. All 112 existing tests continue to pass.
</objective>

<execution_context>
@/Users/chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/chaitanya/Documents/github/terminair/.planning/PROJECT.md
@/Users/chaitanya/Documents/github/terminair/.planning/ROADMAP.md
@/Users/chaitanya/Documents/github/terminair/.planning/phases/04-screens/04-RESEARCH.md
@/Users/chaitanya/Documents/github/terminair/.planning/phases/04-screens/04-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from the three files being modified. Read these before touching any file. -->

From terminair/screens/model_list.py (current state):
- Class: ModelListScreen(DbtScreen) in terminair/screens/model_list.py
- compose() yields into Vertical: Static("dbt models", id="model-list-header"), Static("", id="model-list-meta"), Static("", id="model-list-tags"), FilterInput(), DataTable(id="model-table")
- on_mount(): async def on_mount(self) -> None: await self._load_models()
- _render() ends with: self._update_meta(); self._update_tag_bar()
- Imports: from rich.text import Text; from textual.binding import Binding; from textual.containers import Vertical; from textual.widgets import DataTable, Static; from terminair.dbt.models import ModelState; from terminair.screens.base import DbtScreen; from terminair.widgets.filter_input import FilterInput
- CSS block ends just before BINDINGS at line 60; add #model-list-status CSS rule inside the CSS string

From terminair/screens/lineage.py (current state):
- __init__ at line 52: self._depth = 3  (change to 4, nothing else)

From terminair/screens/detail.py (current state):
- BINDINGS = DbtScreen.BINDINGS + [Binding("enter", "noop", "Open")]
- compose() TabPane("SQL", Static("", id="detail-sql", classes="detail-pane"))  — single-arg form
- RegressionAnalyzer already imported: from terminair.dbt.regression import RegressionAnalyzer
- Imports: from textual.containers import Vertical  — add VerticalScroll to this import
- last method is action_noop() at line 142 — add action_switch_tab() after it

From terminair/screens/base.py (inherited by all screens):
- app_typed property: returns self.app typed as TerminairApp
- _flash_error(msg: str) method available

From terminair/app.py:
- get_config() -> Config: returns self._config
- Config.connections: dict[str, Connection]; Config.settings.default_connection: str
- Connection.url: str (always present on a non-None Connection)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ModelListScreen — Add connection+clock header and regression warning statusbar (SCR-01)</name>
  <files>terminair/screens/model_list.py</files>
  <read_first>
    terminair/screens/model_list.py — Read the full file before making any change. Lines 1-157.
    terminair/screens/problems.py — Lines 1-15 only: confirm the RegressionAnalyzer import line to copy.
  </read_first>
  <action>
Make four targeted edits to terminair/screens/model_list.py. Do NOT rewrite the file. Do NOT alter _render(), action_cycle_tag_filter(), or any DataTable logic.

Edit 1 — Add RegressionAnalyzer import after the existing imports block (after line 10, before class definition):
    from terminair.dbt.regression import RegressionAnalyzer

Edit 2 — Extend the CSS string (inside the triple-quoted CSS = """ block, before the closing """). Add a new CSS rule after the existing #model-list-tags rule:
    #model-list-status {
        height: auto;
        padding: 0 1 1 1;
        color: #a5b4fc;
        background: #24263a;
    }

Edit 3 — Add the new Static to compose(). The Vertical block currently has 5 yields ending with DataTable. Add one more yield as the last item inside the Vertical:
    yield Static("", id="model-list-status")

Edit 4 — Add two new methods and extend on_mount. Insert after the existing _update_tag_bar method (before action_cycle_tag_filter):

    def _update_header(self) -> None:
        config = self.app_typed.get_config()
        conn = config.connections.get(config.settings.default_connection)
        url = conn.url if conn else "demo"
        from datetime import datetime, timezone
        ts = datetime.now(timezone.utc).strftime("%H:%M:%S UTC")
        self.query_one("#model-list-header", Static).update(
            f"dbt models  |  {url}  |  {ts}"
        )

    def _update_statusbar(self) -> None:
        signals = RegressionAnalyzer(self._models).analyze()
        warnings = sum(1 for s in signals if s.severity in ("critical", "warning"))
        self.query_one("#model-list-status", Static).update(
            f"{len(self._models)} models  |  {warnings} regression warnings"
        )

Then extend on_mount to start the 1-second interval AFTER await self._load_models():
    self.set_interval(1.0, self._update_header)

Then extend _render() to call _update_statusbar() after the existing _update_tag_bar() call:
    self._update_statusbar()

CRITICAL: _update_header() must ONLY call query_one("#model-list-header", Static).update(). It must NOT call _render(), _load_models(), or _update_statusbar(). The clock tick path and the data load path are completely separate.
  </action>
  <verify>
    <automated>
      uv run python3 -c "from terminair.screens.model_list import ModelListScreen; print('import ok')" && uv run pytest terminair/tests/ -q --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_update_header\|_update_statusbar\|model-list-status\|RegressionAnalyzer" terminair/screens/model_list.py` returns at least 6 lines
    - `grep -n "set_interval" terminair/screens/model_list.py` returns exactly 1 line inside on_mount
    - `grep -n "_update_statusbar" terminair/screens/model_list.py` returns at least 2 lines (definition + call in _render)
    - `uv run pytest terminair/tests/ -q --tb=short` exits 0 — all 112 tests pass
    - `_update_header()` body does NOT contain any call to `_render`, `_load_models`, or `_update_statusbar` (grep confirms)
  </acceptance_criteria>
  <done>ModelListScreen header displays "dbt models  |  {url}  |  {clock}" and the bottom Static displays "{N} models  |  {M} regression warnings". The 1-second interval updates only the header Static. Regression count updates on every data render cycle.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LineageScreen — Set default depth to 4 hops (SCR-03)</name>
  <files>terminair/screens/lineage.py</files>
  <read_first>
    terminair/screens/lineage.py — Lines 52-57 only: confirm self._depth = 3 at line 55.
  </read_first>
  <action>
Single-line change. In terminair/screens/lineage.py __init__ method, change:
    self._depth = 3
to:
    self._depth = 4

No other changes. The visit() recursion guard `if depth >= self._depth` means _depth=4 renders root (depth 0) through hop 4, satisfying the "4-hop depth default" requirement.
  </action>
  <verify>
    <automated>
      grep -n "_depth = 4" terminair/screens/lineage.py && uv run pytest terminair/tests/ -q --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_depth" terminair/screens/lineage.py` shows `_depth = 4` (not 3) in __init__
    - `uv run pytest terminair/tests/ -q --tb=short` exits 0 — all tests pass
    - No other lines in lineage.py were changed (git diff shows exactly 1 changed line)
  </acceptance_criteria>
  <done>LineageScreen initializes with _depth=4. First render shows root + 4 child levels by default. +/- depth expansion continues to work unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ModelDetailScreen — Add 1-5 tab key bindings and scrollable SQL pane (SCR-04)</name>
  <files>terminair/screens/detail.py</files>
  <read_first>
    terminair/screens/detail.py — Read the full file before making any change. Lines 1-144.
  </read_first>
  <action>
Make three targeted edits to terminair/screens/detail.py. Do NOT rewrite the file. Do NOT alter any _render_* methods or _current_model().

Edit 1 — Extend the VerticalScroll import. The current import at the top reads:
    from textual.containers import Vertical
Change it to:
    from textual.containers import Vertical, VerticalScroll

Edit 2 — Replace BINDINGS. The current BINDINGS is:
    BINDINGS = DbtScreen.BINDINGS + [
        Binding("enter", "noop", "Open"),
    ]
Replace it with:
    BINDINGS = DbtScreen.BINDINGS + [
        Binding("enter", "noop", "Open"),
        Binding("1", "switch_tab('tab-1')", "Status", show=False),
        Binding("2", "switch_tab('tab-2')", "Structure", show=False),
        Binding("3", "switch_tab('tab-3')", "Refs", show=False),
        Binding("4", "switch_tab('tab-4')", "SQL", show=False),
        Binding("5", "switch_tab('tab-5')", "Regression", show=False),
    ]

NOTE on tab IDs: The five TabPanes in compose() have NO explicit id attribute. Textual auto-assigns tab-1 through tab-5 sequentially. Tab-1 = Status, tab-2 = Structure, tab-3 = Variables+Refs, tab-4 = SQL, tab-5 = Regression. show=False keeps the footer uncluttered.

NOTE on binding shadowing: When ModelDetailScreen is active, keys 1-4 trigger tab switching, not screen navigation. This is correct and intentional per SCR-04. It is NOT a bug.

Edit 3 — Replace the SQL TabPane in compose(). The current line is:
    yield TabPane("SQL", Static("", id="detail-sql", classes="detail-pane"))
Replace it with the context-manager form:
    with TabPane("SQL"):
        with VerticalScroll(id="detail-sql-scroll"):
            yield Static("", id="detail-sql", classes="detail-pane")

Edit 4 — Add CSS for VerticalScroll height. Inside the CSS string (before closing """), add after the .detail-pane rule:
    #detail-sql-scroll {
        height: 1fr;
    }

Edit 5 — Add action_switch_tab method. After the existing action_noop() method at the end of the class, add:
    def action_switch_tab(self, tab_id: str) -> None:
        self.query_one("#detail-tabs", TabbedContent).active = tab_id

VERIFY the #detail-sql Static selector still works: _render() calls self.query_one("#detail-sql", Static) — this still works because query_one searches by id through the full widget tree, including inside VerticalScroll.
  </action>
  <verify>
    <automated>
      grep -n "VerticalScroll\|tab-1\|action_switch_tab" terminair/screens/detail.py && uv run pytest terminair/tests/ -q --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VerticalScroll" terminair/screens/detail.py` returns at least 2 lines (import + usage in compose)
    - `grep -n "switch_tab" terminair/screens/detail.py` returns at least 6 lines (5 Binding entries + 1 method definition)
    - `grep -n "detail-sql-scroll" terminair/screens/detail.py` returns at least 2 lines (CSS rule + VerticalScroll id)
    - `grep -n "detail-sql-scroll" terminair/screens/detail.py | grep "1fr"` returns 1 line (height CSS)
    - `uv run python3 -c "from terminair.screens.detail import ModelDetailScreen; print('import ok')"` exits 0
    - `uv run pytest terminair/tests/ -q --tb=short` exits 0 — all tests pass
  </acceptance_criteria>
  <done>ModelDetailScreen BINDINGS has 1-5 keys that call action_switch_tab. action_switch_tab sets TabbedContent.active to "tab-N". The SQL TabPane wraps Static in VerticalScroll with id="detail-sql-scroll" and height: 1fr CSS. All existing _render() selectors (query_one by id) continue to work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen→app | Screen reads Config via app_typed.get_config(); Config is loaded at startup from trusted local files only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | _update_header() displaying connection URL | accept | URL is from local config, not user input; TUI is local-only; no external exposure |
| T-04-02 | Denial of Service | RegressionAnalyzer called on every _render() cycle | accept | analyze() operates on in-memory list[ModelState]; no I/O; O(n) for n=10-100 models; negligible cost |
| T-04-03 | Denial of Service | set_interval(1.0) clock tick calling query_one every second | accept | Single Static.update() per tick; Textual handles diff rendering; no data reload triggered |
</threat_model>

<verification>
After all three tasks complete, run the full suite:

    uv run pytest terminair/tests/ -v --tb=short

Expected: all 112 tests pass (0 failures, 0 errors).

Grep gates (run these to confirm all changes landed):

    grep -n "_depth = 4" terminair/screens/lineage.py
    grep -n "VerticalScroll" terminair/screens/detail.py
    grep -n "action_switch_tab" terminair/screens/detail.py
    grep -n "model-list-status" terminair/screens/model_list.py
    grep -n "set_interval" terminair/screens/model_list.py
</verification>

<success_criteria>
1. SCR-01: terminair/screens/model_list.py has _update_header(), _update_statusbar(), #model-list-status Static in compose(), set_interval in on_mount(), and _update_statusbar() call in _render().
2. SCR-03: terminair/screens/lineage.py __init__ sets self._depth = 4.
3. SCR-04: terminair/screens/detail.py has VerticalScroll wrapping #detail-sql, 5 tab-switching Bindings, action_switch_tab() method, and #detail-sql-scroll height: 1fr CSS.
4. All 112 tests pass: `uv run pytest terminair/tests/ -q --tb=short` exits 0.
5. SCR-02 and SCR-05: No changes made to problems.py or base.py — these remain fully compliant as verified in research.
</success_criteria>

<output>
After completion, create `.planning/phases/04-screens/04-01-SUMMARY.md` using the summary template at @/Users/chaitanya/.claude/get-shit-done/templates/summary.md
</output>
