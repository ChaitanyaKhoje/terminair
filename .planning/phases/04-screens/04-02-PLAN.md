---
phase: 04-screens
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: []
autonomous: false
requirements: [SCR-01, SCR-02, SCR-03, SCR-04, SCR-05]

must_haves:
  truths:
    - "ModelListScreen shows connection URL and live clock in header"
    - "ModelListScreen shows model count and regression warning count in bottom statusbar"
    - "LineageScreen renders 4 child levels deep by default without pressing +"
    - "ModelDetailScreen 1-5 keys switch between the correct tabs"
    - "ModelDetailScreen SQL tab scrolls when compiled SQL exceeds terminal height"
    - "ProblemsScreen two-section layout (failures + signals) unchanged and working"
    - "All screens respond to /, Esc, r, :, q"
  artifacts:
    - path: "terminair/screens/model_list.py"
      provides: "ModelListScreen with SCR-01 gaps closed"
      contains: "_update_header"
    - path: "terminair/screens/lineage.py"
      provides: "LineageScreen with _depth=4"
      contains: "_depth = 4"
    - path: "terminair/screens/detail.py"
      provides: "ModelDetailScreen with tab keys and scrollable SQL"
      contains: "VerticalScroll"
  key_links:
    - from: "human running --demo"
      to: "all 4 screens"
      via: "uv run python3 -m terminair --demo"
      pattern: "N/A — manual verification"
---

<objective>
Manual smoke verification of all five SCR requirements in live --demo mode. Confirms that the gap fixes from Plan 01 work correctly in the running TUI, and that SCR-02 and SCR-05 (pre-implemented, not touched in Plan 01) remain fully functional.

Purpose: Phase gate before /gsd-verify-work. All five requirements must be observable by a human in the terminal.
Output: Human confirmation that all 5 SCR requirements pass in demo mode.
</objective>

<execution_context>
@/Users/chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/chaitanya/Documents/github/terminair/.planning/ROADMAP.md
@/Users/chaitanya/Documents/github/terminair/.planning/phases/04-screens/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated regression gate before smoke</name>
  <files></files>
  <read_first>
    .planning/phases/04-screens/04-01-SUMMARY.md — confirm Plan 01 completed and all tasks passed.
  </read_first>
  <action>
Run the full test suite to confirm Plan 01 introduced no regressions:
    uv run pytest terminair/tests/ -v --tb=short

If any test fails, do NOT proceed to the checkpoint. Fix the regression first by reviewing the diff introduced in Plan 01.

Also run the import smoke check:
    uv run python3 -c "from terminair.screens.model_list import ModelListScreen; from terminair.screens.lineage import LineageScreen; from terminair.screens.detail import ModelDetailScreen; print('all imports ok')"
  </action>
  <verify>
    <automated>
      uv run pytest terminair/tests/ -v --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - All tests pass (exit 0); test count is 112 or higher
    - Import smoke check prints "all imports ok" with exit 0
  </acceptance_criteria>
  <done>No test regressions from Plan 01 changes. Safe to launch --demo for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke — verify all 5 SCR requirements in --demo mode</name>
  <read_first>
    .planning/ROADMAP.md — Phase 4 success criteria (lines 86-92) for exact expected behavior.
  </read_first>
  <what-built>
    Plan 01 closed four gaps across three screen files:
    - ModelListScreen: connection+clock in header; model count + regression warnings in bottom statusbar
    - LineageScreen: default depth changed from 3 to 4 (shows 4 child levels without pressing +)
    - ModelDetailScreen: keys 1-5 switch tabs; SQL tab is scrollable via VerticalScroll
    SCR-02 (ProblemsScreen) and SCR-05 (shared bindings) were pre-implemented and untouched.
  </what-built>
  <how-to-verify>
Start the demo:
    uv run python3 -m terminair --demo

Then verify each requirement:

SCR-01 (ModelListScreen — key 1):
  1. App starts on ModelListScreen by default. Header shows "dbt models  |  {url}  |  {HH:MM:SS UTC}". Clock updates every second.
  2. Bottom of the screen shows "{N} models  |  {M} regression warnings".
  3. Press t repeatedly — tag filter cycles (all → finance → marketing → core → risk → all).
  4. Press / — filter bar opens at top of DataTable. Type a partial model name — table filters live.
  5. Verify DataTable has 8 columns: status, model, tag, status_text, duration, rows, row_delta, dag_id.

SCR-02 (ProblemsScreen — key 2):
  6. Press 2. Two sections visible: "Active Failures" and "Regression Signals".
  7. Failures section shows "upstream" (yellow) or "self" (red) in cause column.
  8. Signals section shows severity colors: critical=bold red, warning=bold yellow, info=dim.

SCR-03 (LineageScreen — key 3):
  9. Press 3. ASCII tree renders immediately with at least 4 levels deep (root + 4 child levels) without pressing +.
  10. Press + — tree goes deeper. Press - — tree collapses. Press m — model mode. Press g — group mode (flat DAG-layer list).

SCR-04 (ModelDetailScreen — Enter from any screen):
  11. Navigate back to screen 1 (press 1). Press Enter on any model row.
  12. Detail screen opens with 5 tabs visible: Status, Structure, Variables+Refs, SQL, Regression.
  13. Press 1 — Status tab active. Press 2 — Structure tab active. Press 3 — Variables+Refs tab active. Press 4 — SQL tab active. Press 5 — Regression tab active.
  14. On SQL tab: if compiled SQL is long (more than terminal height), verify you can scroll down with arrow keys or mouse scroll.

SCR-05 (All screens):
  15. From ModelDetailScreen, press Esc — returns to the previous list screen. Note the cursor row position before pressing Enter; verify cursor is still on the same row after Esc.
  16. On any screen, press r — screen refreshes.
  17. On any screen, press : — command palette opens.
  18. On any screen, press q — app exits cleanly.
  </how-to-verify>
  <resume-signal>
Type "approved" if all 18 verification steps pass, or describe which steps failed and what behavior was observed instead.
  </resume-signal>
  <acceptance_criteria>
    - All 18 steps above pass without error or unexpected behavior
    - No Python tracebacks appear in the terminal during the smoke run
    - App exits cleanly on q (no hanging processes)
  </acceptance_criteria>
  <done>All five SCR requirements (SCR-01 through SCR-05) are visually confirmed working in --demo mode. Phase 4 is ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo mode | MockDataProvider only; no external services; no real credentials in transit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | --demo mode MockDataProvider | accept | Demo mode is local-only; MockDataProvider has no real credentials; no external surface |
</threat_model>

<verification>
Automated gate in Task 1 must pass before Task 2 (the checkpoint) is presented.

    uv run pytest terminair/tests/ -v --tb=short

All tests green = safe to smoke.
</verification>

<success_criteria>
1. pytest exits 0 with all 112+ tests passing.
2. Human confirms all 18 manual verification steps in --demo mode.
3. No screen produces a Python traceback or FlashBar error during the smoke run.
4. Phase 4 is marked complete and ready for Phase 5.
</success_criteria>

<output>
After completion, create `.planning/phases/04-screens/04-02-SUMMARY.md` using the summary template at @/Users/chaitanya/.claude/get-shit-done/templates/summary.md
</output>
